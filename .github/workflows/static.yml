# Workflow simple para el despliegue estáticof en GitHub Pages
name: Deploy Static Content to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

# Permisos de lectura al token para el despliegue
permissions:
  contents: read
  pages: write
  id-token: write

# Asegura que solo se ejecute un despliegue a la vez.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # El trabajo de despliegue
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Carpeta que se subirá: la raíz del repositorio (donde está index.html)
          path: './' 

  # Despliegue final
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build # Asegura que el build termine antes de desplegar
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
